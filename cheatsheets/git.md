Git Cheat Sheet
To check its status, do : git status
You will see untracked files, staged files, and changes to commit.

To add file : git add file.txt
To commit changes : git commit -m "Initial comment"